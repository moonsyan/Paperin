# 更新日志

本文件只记录已经发生的版本事实。`0.7.0` 是第一个打 tag 的候选版本。推送 `v0.7.0` 只会生成 GitHub Draft Release；安装、升级和卸载循环仍未在隔离环境完成，不能把这份日志当成“已公开稳定发布”。

## 未发布 — 代码块语法高亮语义色板（2026-09-23）

- 代码高亮改为 `--code-*` 语义变量（浅/深两套底色，关键字跟主题 accent）；扩展常用语言注册（toml/tsx/docker/json5 等）与标识归一化（含 JSON/Yaml 大小写与别名）。

## 未发布 — 右侧面板与轻量大纲可拖宽（2026-09-23）

- 去掉 soft-workbench 对 ContextDock 的 300px 封顶；拖拽条不再被 `overflow` 裁切，完整面板可在 260–420px 改宽。
- 进入轻量大纲时收到建议窄栏（≤240），之后可拖 200–420px；dock 状态更新改为函数式 setState。

## 未发布 — 演示 Wiki、假 dirty 与关闭确认（2026-09-23）

- 演示树 Wiki 链接可解析并跳转；Wiki 转换在基线前同步，避免未改却 dirty。
- 标签切换用 initialize 基线；关闭确认只认文档会话，不再比对编辑器句柄身份（修复新建空白文档「保存/不保存」无效）。

## 未发布 — GPU 白屏软栅回退（2026-09-23）

- Windows 上 GPU 子进程崩溃后 DOM 仍可运行但窗口只剩系统标题栏白屏时，写入 `userData/gpu-fallback` 并在下次启动（必要时自动 relaunch）改用软栅；也可用环境变量 `PAPERIN_DISABLE_GPU=1` 强制关闭硬件加速。

## 未发布 — 新手入门与可见契约收口（2026-09-23）

- 欢迎「小任务」区分无库熟悉界面与开库后的搜索/插入引用/保存闭环；同步拼写检查已生效口径。
- 开始页有知识库且存在本库最近文件时，主按钮为「继续最近编辑」。
- 同步 README、隐私、第三方说明、UI/工作流/发行入口与设置提示；清理「更多示例」中过时的未完成待办勾选。

## 未发布 — 产品复审与全量文档同步（2026-09-23）

- 新增 [产品战略发展报告](docs/PRODUCT-STRATEGY-REVIEW-2026-09-23.md) 与 [状态审查证据](docs/development/reviews/2026-09-23-product-state-audit.md)；旧 09-22 战略正文改为兼容入口。
- 按代码与本机门禁重核：旧来源/索引/缓存补强已落地；新增 A01–A10（状态目录越界、来源身份/路径、测试装配、性能红灯、拼写检查无效等）。
- **全量同步**用户说明、维护契约、领域/UI/发行/验收协议与文档索引；删除决策：无废稿（stub 与冻结审阅保留）。
- 同步应用欢迎正文与设置「拼写检查」提示中的 A09 边界；性能/主题原始 JSON 未改；产品业务逻辑未改。
- 默认测试因安装脚本 shebang 的 Vite SSR 转换失败；`perf:production` / `perf:regression` 保持红灯；不把历史绿灯或未重跑结果写成新鲜通过。

## 未发布 — 文档同步（2026-09-22）

- 全量核对用户说明、维护契约、战略与实施计划，区分已实现、待实施和未验证。
- 补充**当时**的来源共享基线说明、索引与缓存待办、恢复备份的数据范围；同步仓库与应用内入门说明。
- 统一种子观察、Alpha 发现轮、Beta 确认轮与 W2/W4 留存口径；修正旧快捷键和文档表格。
- 第二遍交叉审计：拆分战略审查日证据与文档同步复验，避免把未重跑的 audit/perf 写成新鲜通过；状态表标明来源健康为共享初版；SECURITY 区分 backup 与 journal；忽略 `**/.paperin/`。
- 当时不实现来源/索引修复，不升级版本、不创建 tag、不发布安装包；历史验证结果不因文档更新变为新鲜通过。后续实现与 2026-09-23 审查见上一节。

## 0.7.0 — 2026-09-21

### 首次使用

- 第一次打开时，「欢迎使用.md」按模块说明打开资料、写成文档、找来源并引用、保存与继续维护、检查质量、交付、界面，以及数据放在哪里。
- 仓库里的 [`docs/getting-started.md`](docs/getting-started.md) 与这份欢迎正文保持一致。

### 写作与交付

- 可以从资料搜索或反向链接插入引用，保存后关闭再打开仍能回到同一篇文档。
- 质量检查会提示来源已变化、来源缺失或索引未完成；不会自动改正文里的链接。
- 发布可以选择模板和范围，工作区最多保存 20 条配置，配置不含正文。
- HTML 资源包包含 `reports/paperin-delivery-report.json`，报告只有诊断计数和相对路径。

### 安全与发行材料

- 生产环境默认检查更新，设置里可以关闭；开发环境不检查更新。
- 图片默认留在本地。只有主动配置 SM.MS 后才会上传，凭据不进入渲染进程。
- 仓库提供 `LICENSE`、`THIRD-PARTY-NOTICES.md`、`PRIVACY.md`、`SECURITY.md` 和 [`CONTRIBUTING.md`](CONTRIBUTING.md)。

### 这次没有验证

- Windows 两个隔离环境的安装、升级、卸载、文件关联和用户文件保留。
- macOS 签名与公证，Linux 桌面集成。
- 第二台设备上的 8 小时稳定性，以及真人 15 分钟任务样本。
