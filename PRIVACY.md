# 隐私说明

Paperin 是本地桌面应用。Markdown 正文默认只写在用户指定的本地文件里。应用不要求登录，也不会把正文作为遥测上报。本说明描述**当前代码实际会发生的联网**，不是独立审计报告。

更新开关在**下次启动**时完全生效；图片模式和拼写检查的关闭方式见各自条目。应用不会因本次文档更新增加联网。

## 本地保存的数据

- 用户文档：用户打开或保存的 `.md` 文件，位置由用户选择。
- 应用设置、草稿、最近文件、收藏、写作统计：与正文分离，写在 Electron `userData` 目录（Windows 一般为 `%APPDATA%\Paperin`）。
- 工作区私有状态：知识库内 `.paperin/settings.json`、`workspace.json`、`documents.json` 保存主题、附件目录、搜索词、相对引用路径、最多 50 条共享来源时间记录、最多 20 条发布配置、标签布局和阅读位置。删除该目录不删除 Markdown，但会丢失这些状态；不应提交或作为普通文档公开。
- 索引缓存与版本历史：位于应用私有数据目录。索引可能含本地路径、标题、标签、链接等派生信息，不等于匿名数据；版本历史及草稿含正文，均应按用户数据保管。缓存可重建，版本历史不等于独立备份。
- 保存恢复材料：正文同目录的隐藏 backup 含上一个确认版本的正文，临时文件含待保存正文；journal 保存阶段、进程标识和哈希，不含正文或绝对路径。成功后清理，失败时可能保留以便恢复；不要公开上传或随意删除。
- SM.MS token：只存主进程，经操作系统安全存储加密（Windows 为 DPAPI / Electron `safeStorage`）。渲染进程只能看到是否已配置，拿不到明文。

卸载安装包**预期**不删除用户自己的知识库文件夹；该行为尚未在隔离环境验证，见 [发行验证](docs/development/release-validation.md)。

## 三类联网路径

### 1. 更新检查、自动下载、退出时安装（生产环境默认开启）

- **何时：** 生产构建启动时。开发环境永不检查。
- **做什么：** 使用 `electron-updater` 向配置的 GitHub 发布源（`package.json` 中 `publish.owner=moonsyan`、`publish.repo=Paperin`）检查是否有新版本；若有则自动下载；退出时安装已下载的包。
- **发送内容：** 更新协议所需的版本与平台信息。**不上传** Markdown 正文、草稿、路径或 token。
- **如何关闭：** 设置 → 高级 →「自动检查更新」。关闭后下次启动不检查、不自动下载，也不会在退出时安装此前已下载的包。
- **第三方：** GitHub 按自身条款处理更新请求。

### 2. SM.MS 图片上传（默认关闭，用户主动启用）

- **何时：** 用户在设置中把图片位置改成 SM.MS 并成功保存 token 后，粘贴或拖入图片且状态为已配置。
- **做什么：** 主进程把图片发往 `https://sm.ms/api/v2/upload`，请求头带 token。
- **发送内容：** 图片二进制与 token。不附带当前文档正文。
- **失败时：** 上传、解密、迁移或安全存储失败时改用本地附件，不静默丢弃明文（迁移失败时保留明文直至加密成功）。
- **如何关闭：** 改回「本地附件」，或不要配置 token。
- **第三方：** 上传后的图片由 SM.MS 按其服务条款处理；Paperin 无法控制其留存与再分发。

### 3. 拼写检查词典（默认关闭）

- **何时：** 用户在设置中打开「拼写检查」。
- **做什么：** Electron/Chromium 可能下载所选语言的拼写词典。
- **发送内容：** 词典下载请求。**不上传**正文。
- **如何关闭：** 关闭拼写检查开关（默认即为关闭）。中文不在内置词典范围。

## 不会默认做的事

本地 HTML 资源包的交付报告可能包含缺失目标的相对路径与诊断计数；“不含正文、绝对路径、搜索词”不等于完全匿名，分享前应检查报告。可在设置 → 高级或命令面板打开「支持摘要」：先预览脱敏 JSON（版本、平台、索引诊断计数、本地枚举事件计数与近期错误码），再由你主动复制或导出；不读取日志正文，不含路径或正文，没有自动研究上传或自动发送支持包。

- 不把 token、绝对路径、草稿正文写入本仓库、调试日志或错误上报通道。
- 不把自定义协议或图片 Data URL 未经验证就插入 DOM、打印窗口或文件系统。
- 当前没有账号系统、云同步或远程 AI；若以后增加，会在发送前说明范围，且无模型时本地编辑与导出仍可用。
